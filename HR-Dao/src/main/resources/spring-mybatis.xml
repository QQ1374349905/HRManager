<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans.xsd
http://www.springframework.org/schema/tx
http://www.springframework.org/schema/tx/spring-tx.xsd
http://www.springframework.org/schema/aop
http://www.springframework.org/schema/aop/spring-aop.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context.xsd">


    <!--引用配置文件-->
    <context:property-placeholder location="classpath:jdbc.properties"/>

    <!--配置数据源-->
    <bean class="com.mchange.v2.c3p0.ComboPooledDataSource" id="dataSource">
        <property name="maxPoolSize" value="${size}"/>
        <property name="driverClass" value="${driver}"/>
        <property name="jdbcUrl" value="${url}"/>
        <property name="user" value="${user}"/>
        <property name="password" value="${password}"/>
    </bean>

    <!--创建SqlSessionFactory-->
    <bean class="org.mybatis.spring.SqlSessionFactoryBean" id="factoryBean">
        <!--指定关键配置文件-->
        <property name="configLocation" value="classpath:mybatis-config.xml"/>
        <!--指定数据源-->
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!--自动扫描mapper包下的类自动生成代理注入spring-->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer" id="mapperScannerConfigurer">
        <property name="basePackage" value="com.qnck.dao"/>
    </bean>

    <!--声明式事务控制-->
    <bean class="org.springframework.jdbc.datasource.DataSourceTransactionManager" id="dataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!--配置事务通知-->
    <tx:advice transaction-manager="dataSourceTransactionManager" id="advice">
        <!--tx:attributes配置所有的事务传播规则-->
        <tx:attributes>
            <!--tx:method 配置每个方法的规则-->
            <!--指定会员和会员信息表使用事务删除数据-->
            <tx:method name="delete*" propagation="REQUIRED"/>
            <!--给添加数据的方法使用事务-->
            <tx:method name="add*" propagation="REQUIRED"/>
            <!--给修改数据的方法添加事务-->
            <tx:method name="update*" propagation="REQUIRED"/>
        </tx:attributes>
    </tx:advice>

    <!--将通知应用到节点-->
<!--    <aop:config>-->
<!--        <aop:pointcut id="pointcut" expression="execution(* com.ck.service.*.*(..))"/>-->
<!--        <aop:advisor advice-ref="advice" pointcut-ref="pointcut"/>-->
<!--    </aop:config>-->


</beans>